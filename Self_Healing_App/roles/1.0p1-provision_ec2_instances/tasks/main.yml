---
- name: Provision EC2 instance
  ec2:
    keypair: "{{ keypair }}"
    instance_type: t2.micro
    image: "{{ ami }}"
    wait: yes
    group: Web Servers
    count: 1
    vpc_subnet_id: "{{ vpc_subnet }}"
    region: "{{ region }}"
    assign_public_ip: yes
    instance_tags:
      Name: "{{ student }}-{{ inventory_hostname }}"
      owner: "{{ student }}"
  register: ec2

- name: Dump stored facts
  debug:
    msg: "info  {{ ec2.instances }}"

- name: Tower | Add new instance to host group
  add_host:
    hostname: "{{ item.ec2_tag_Name }}"
    ansible_host: "{{ item.ec2_public_dns_name }}"
    private_ip: "{{ item.ec2_private_ip_address }}"
    groups: "{{ student }}"
  with_items: "{{ ec2.instances }}"

- name: Wait for SSH to come up
  wait_for:
    host: "{{ item.public_ip }}"
    port: 22
    delay: 60
    timeout: 320
    state: started
  with_items: "{{ ec2.instances }}"
